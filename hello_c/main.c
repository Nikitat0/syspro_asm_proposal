#include "library.h"

#include <stdio.h>

int main(int argc, char** argv) {
  int a, b, c;
  int x, y, z;

  scanf("%d %d %d", &a, &b, &c);
  scanf("%d %d %d", &x, &y, &z);

  // Place your code below

  // What you can't do: use any arithmetic operators, call any function except functions from "library.h"
  // What you can do: call functions from "library.h", create variables, assign values to variables

  return 0;
}
